import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (tester) async {
    // Placeholder — camera/Gemini tests require device/API mocking.
    expect(true, isTrue);
  });
}
